USER=pi
# Backup server address.
ADDRESS=somewhere.com
SSH_PORT=22
# Name of backup.
BACKUP_NAME=computer1backup
# Array of directories to backup.
DIRECTORIES=($HOME/Documents $HOME/Pictures)
# Limit bandwidth (Optional).
# BWLIMIT=1000
